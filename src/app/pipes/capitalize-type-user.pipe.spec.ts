import { CapitalizeTypeUserPipe } from './capitalize-type-user.pipe';

describe('CapitalizeTypeUserPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizeTypeUserPipe();
    expect(pipe).toBeTruthy();
  });
});
